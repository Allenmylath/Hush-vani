# Workspace root. `hush-vani` is the only crate; `ort/` is Python, not a crate.
[workspace]
members = ["hush-vani"]
resolver = "2"

# Release profile applies to every member; a member's own [profile.release] would be ignored.
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
